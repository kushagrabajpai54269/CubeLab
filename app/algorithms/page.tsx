import { ListChecks } from 'lucide-react'
import { PlaceholderPage } from '@/components/shared/PlaceholderPage'

export default function AlgorithmsPage() {
  return (
    <PlaceholderPage
      title="Find the algorithm you need"
      description="A searchable, filterable algorithm encyclopedia lands in Phase F, built on the shared solver case-table data (D-011)."
      icon={ListChecks}
    />
  )
}
