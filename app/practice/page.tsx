import { Repeat } from 'lucide-react'
import { PlaceholderPage } from '@/components/shared/PlaceholderPage'

export default function PracticePage() {
  return (
    <PlaceholderPage
      title="Practice specific cases"
      description="Drill OLL, PLL, F2L, and Cross cases in isolation, in its own module sharing only the core cube state with the Solver (D-012)."
      icon={Repeat}
    />
  )
}
